import enforceAltTags from "./enforceAltTags";
import noGenericAltValues from "./noGenericAltValues";

const rules = {
	"enforce-alt-tags": enforceAltTags,
	"no-generic-alt-values": noGenericAltValues
};

export default rules;
